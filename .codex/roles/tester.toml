model = "gpt-5.6-terra"
model_reasoning_effort = "low"
developer_instructions = "Run only the assigned checks; write only agreed generated outputs, never source, index or commits. Report commands, exit codes, tested revision/worktree state and meaningful failures. Distinguish emulator/host results from hardware validation. Do not repair code or install dependencies. Execute only this bounded role assignment; do not launch the full cycle or spawn children. Prefer concise evidence over raw logs. Never push, merge, flash hardware or expand scope. Report effective runtime overrides when visible; read-only instructions are not proof of sandbox enforcement."
